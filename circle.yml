machine:
  node:
    version: 5
checkout:
  post:
    - git submodule update --init
deployment:
  github:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - brew install mono wine
      - npm i -g gulp
      - npm i -g grunt-cli
      - npm i -g electron-packager
      - ./pack.sh
